## 1. 后端基础

- [x] 1.1 添加后端配置，包括 SQLite 数据库路径、音频存储路径、允许的音频 MIME type 和最大上传大小。
- [x] 1.2 创建录音元数据模型，字段包含 ID、原始文件名、展示名、存储文件名、内容类型、大小、时长、存储路径和创建时间。
- [x] 1.3 添加 `recordings` 表的 SQLite 初始化逻辑。

## 2. 后端 API

- [x] 2.1 实现 `POST /api/recordings`，包含 multipart 上传校验和安全存储文件名生成。
- [x] 2.2 将通过校验的音频文件保存到 `storage/audio/`，并创建对应的 SQLite 元数据记录。
- [x] 2.3 实现 `GET /api/recordings`，按最新上传优先返回录音列表。
- [x] 2.4 实现 `GET /api/recordings/{recording_id}`，返回单条录音或 `404`。
- [x] 2.5 实现 `GET /api/recordings/{recording_id}/audio`，返回浏览器可播放的音频响应或 `404`。
- [x] 2.6 添加后端 API 测试，覆盖有效上传、无效上传、列表、详情和音频响应行为。

## 3. 前端体验

- [x] 3.1 添加前端 API client，支持录音上传、列表、详情和音频 URL 构造。
- [x] 3.2 构建上传表单，包含文件选择、提交状态、校验反馈和成功处理。
- [x] 3.3 构建录音列表，包含加载、空、错误和已上传状态。
- [x] 3.4 将录音选择行为连接到 HTML audio 播放器，并使用后端音频端点。
- [x] 3.5 上传成功后刷新录音列表，不要求用户刷新页面。

## 4. 验证

- [x] 4.1 验证受支持的音频文件可以在本地完成上传、列表展示、选择和播放。
- [x] 4.2 验证无效上传不会创建已保存文件或元数据记录。
- [x] 4.3 运行后端测试和前端 lint/build 检查。
- [x] 4.4 更新开发说明，写清楚本地运行命令以及存储/数据库位置。
